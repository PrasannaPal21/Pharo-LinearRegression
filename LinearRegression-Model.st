CMAbstractModel subclass: #LRModel	instanceVariableNames: ''	classVariableNames: ''	package: 'LinearRegression-Model'!"-- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- "!LRModel class	instanceVariableNames: ''!!LRModel class methodsFor: 'as yet unclassified'!prefix	"Every class name in model package must begin with this prefix"	^ 'LR'.! !!LRModel class methodsFor: 'as yet unclassified'!aboutThisModel	"Use this method to provide a detailed description of your model in Microdown"	^ ''! !Object subclass: #LRLinearRegression	instanceVariableNames: 'slope intercept'	classVariableNames: ''	package: 'LinearRegression-Model'!!LRLinearRegression methodsFor: 'as yet unclassified' stamp: '4/1/2025 21:39'!trainX: xValues Y: yValues    | n sumX sumY meanX meanY sumXY sumX2 |    n := xValues size.    sumX := xValues inject: 0 into: [:sum :each | sum + each].    sumY := yValues inject: 0 into: [:sum :each | sum + each].    meanX := sumX / n.    meanY := sumY / n.    sumXY := 0.    sumX2 := 0.    1 to: n do: [:i |        | xi yi |        xi := xValues at: i.        yi := yValues at: i.        sumXY := sumXY + ((xi - meanX) * (yi - meanY)).        sumX2 := sumX2 + ((xi - meanX) squared).    ].    slope := sumXY / sumX2.    intercept := meanY - (slope * meanX).! !!LRLinearRegression methodsFor: 'as yet unclassified' stamp: '4/1/2025 21:42'!predict: x    ^ intercept + (slope * x)! !!LRLinearRegression methodsFor: 'accessing' stamp: '4/1/2025 21:53'!slope    ^ slope! !!LRLinearRegression methodsFor: 'accessing' stamp: '4/1/2025 21:53'!intercept    ^ intercept! !